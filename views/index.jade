extends layout

block content
  h1 Find your train!
  p Kube change!!  Select your train below to see its current schedule.  More kube changes
  p more.. ok
  #wrapper
    #trainList
      h2 Trains
      #trains.d-flex.flex-row
    #trainInfo
      strong  <span id='trainName'></span>
      #trainSchedule
        strong Select a train to view its current schedule.
